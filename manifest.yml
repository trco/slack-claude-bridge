display_information:
  name: claude-YOURNAME
features:
  bot_user:
    display_name: claude-YOURNAME
    always_online: true
oauth_config:
  scopes:
    bot: [app_mentions:read, chat:write, channels:history, groups:history]
settings:
  event_subscriptions:
    bot_events: [app_mention]
  socket_mode_enabled: true
  interactivity:
    is_enabled: false
